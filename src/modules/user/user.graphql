type User {
  _id: ID
  email: String
  password: String
  firstName: String
  lastName: String
}

type Query {
  users: [User]
}

type Mutation {
  createUser( user:  UserInput): Boolean
  login(loginInput: UserLoginInput): LoginRes
}

input UserInput {
  email: String!
  password: String!
  firstName: String!
  lastName: String!
}

type LoginRes {
  status: String
  message: String
  token: String
}

input UserLoginInput {
  email: String
  password: String
}
